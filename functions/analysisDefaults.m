function defaults = analysisDefaults()
% analysisDefaults Central defaults for corrected analysis behavior.

defaults.pipelineVersion = '2026-05-corrected';
defaults.rewardWindowSec = 2;
defaults.plotShiftBaseline = false;
defaults.plotShiftLabel = 'plot-only baseline shift';

end
